# Gemfile for Redmine CMDB plugin
# Purpose: Plugin-specific gem dependencies

gem 'listen', '~> 3.3'

# Bullet is used in Redmine's development.rb
# Adding it here prevents NameError during tests
gem 'bullet', '>= 0', require: false
